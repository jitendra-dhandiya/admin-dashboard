{
    "version": 2,
    "builds": [
      { "src": "frontend", "use": "@vercel/static", "out": "/" },  
      { "src": "backend", "use": "@vercel/node", "config": { "watch": ["index.js"] } }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/index.html" } 
    ]
  }
  